### Jupyter Notebooks

This repository uses `nbstripout` via `pre-commit` to remove execution output
from Jupyter notebooks before commits.

If you haven’t already:

```bash
pip install pre-commit nbstripout
pre-commit install
```
